const exampleSpec = `
// paste spec here

// example:

describe('test D', () => {

	before(() => {
		beforeCommand();
	});

	it('should test 1', () => {
		command1();
	});

	it('should test 2', () => {
		command2();
	});

});
`;

export default exampleSpec;
